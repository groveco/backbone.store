(function(root, factory) {
if (typeof define === 'function' && define.amd) {
define(['jquery', 'underscore', 'backbone', 'rsvp'], factory);
} else if (typeof exports === 'object') {
module.exports = factory(require('jquery'), require('underscore'), require('backbone'), require('rsvp'));
} else {
root.BackboneStore = factory(root.$, root._, root.Backbone, root.RSVP);
}
}(this, function($, _, Backbone, RSVP) {
var window = {};
window.$ = $;
window._ = _;
window.Backbone = Backbone;
window.RSVP = RSVP;
<%= contents %>
}));